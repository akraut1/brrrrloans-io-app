export interface BsInvestorDeal {
  deal_id: string; // bigint in Supabase, string in TypeScript
  contact_id: string; // bigint in Supabase, string in TypeScript
  contact_types_id: string; // bigint in Supabase, string in TypeScript
}
