"use client";

// Import Clerk components directly
import {
  UserButton,
  SignIn,
  SignUp,
  SignInButton,
  SignUpButton,
} from "@clerk/nextjs";

// Re-export the components to maintain the same interface
export { UserButton, SignIn, SignUp, SignInButton, SignUpButton };
